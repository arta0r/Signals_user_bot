matplotlib>=3.7
# Nothing else is required: data comes from free public endpoints, Telegram is plain HTTPS.
# Optional, only if you switch symbols to provider "twelvedata": nothing extra, key goes in secrets.
